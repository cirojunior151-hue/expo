---
Name: jsi

Namespaces:
- Name: facebook
  Namespaces:
  - Name: jsi
    Tags:
    # Runtime is an abstract C++ class with pure virtual methods (not callable on
    # Swift value types) and is managed externally. Import as an immortal reference
    # type (SWIFT_IMMORTAL_REFERENCE) so Swift treats it like a class without
    # attempting to manage its lifetime via retain/release.
    - Name: Runtime
      SwiftImportAs: reference
      SwiftReleaseOp: immortal
      SwiftRetainOp: immortal
    # IRuntime is the abstract base introduced in RN 0.86. Most JSI value/string/
    # property methods now take IRuntime&; without this entry Swift refuses to
    # import IRuntime because the type is abstract (and pre-RN-fix, because its
    # destructor was protected).
    - Name: IRuntime
      SwiftImportAs: reference
      SwiftReleaseOp: immortal
      SwiftRetainOp: immortal
